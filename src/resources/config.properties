# Database Settings
db.address=jdbc:mysql://127.0.0.1:3306/OSI-TEST
db.host=localhost
db.port=3306
db.name=OSI-TEST
#Credentials are not stored here for security reasons and are handled via environment variables or secure vaults, and SQL database authentication mechanisms.
db.username=admin
db.password=password123

# Application Settings
app.debug=true
app.port=8080
app.logLevel=INFO
app.maxConnections=100

# Security Settings
security.enableSSL=true 
security.allowedIPs=""
security.sessionTimeout=30

# Email Settings
email.smtpServer=smtp.example.com
email.smtpPort=587
